import { IUser } from "../users";

export function shouldGenerateRecommendation(user: IUser) {
    // check to see if this person has been active in the last 7 days
        // if they have

    // if they haven't
}
